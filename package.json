{
  "name": "botkit-shujinosuke",
  "version": "1.0.0",
  "description": "",
  "main": "bot.js",
  "private": true,
  "engines": {
    "node": "10",
    "yarn": "1.22"
  },
  "scripts": {
    "postinstall": "test -e .env || cp .env.template .env",
    "start": "if [ $NODE_ENV == 'production' ]; then node src/bot.js; else nodemon --config .nodemon.json; fi"
  },
  "dependencies": {
    "botbuilder-adapter-slack": "^1.0.10",
    "botbuilder-storage-mongodb": "^0.9.5",
    "botkit": "^4.6.2",
    "botkit-plugin-cms": "^1.0.3",
    "dotenv": "^7.0.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.2"
  }
}
